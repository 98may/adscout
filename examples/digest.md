# Example 3 — Digest mode

> Generated by running the SKILL.md digest mode on the real corpus, 2026-07-16.

**Trigger:** *"generate digest for this week's crawl"* (first crawl — all 15 corpus items are new; note that a curated seed crawl naturally clears the threshold more often than a routine weekly one will).

---

## Digest — week of 2026-07-13

### modeling

| Score | Tier | Item |
|---|---|---|
| 8/10 | `[T2 eng blog]` | **REA (Meta)** — autonomous agent runs the ads-ranking experiment loop end-to-end; claimed 2x model accuracy / 5x output (claimed by Meta). Directly hits our `automated_hypothesis_iteration` known gap. |
| 8/10 | `[T2 eng blog]` | **GEM (Meta)** — LLM-scale ads foundation model with fleet-wide knowledge transfer; claimed +5%/+3% conversions (claimed by Meta). |
| 8/10 | `[T4 writeup of T1]` | **HSRL (Kuaishou, WWW 2026)** — RL over fixed semantic-ID action space, deployed in ads ranking; claimed +18.4% advertiser expected spend (claimed by Kuaishou; secondary source — verify against primary). |
| 7/10 | `[T1 paper]` | **LongRetriever (ByteDance)** — ultra-long user sequences moved into the *retrieval* stage (in-context training + multi-context retrieval); deployed at billions-of-users scale. Adjacent to our `two_tower_ann`. |
| 7/10 | `[T2 eng blog]` | **Contextual Sequential Two-Tower (Pinterest)** — real-time context injected into the query tower; claimed ~0.7% ROAS lift (claimed by Pinterest). Adjacent to our `user_sequence_modeling`. |

### ai_era_ads

| Score | Tier | Item |
|---|---|---|
| 8/10 | `[T4 analyst]` + `[T3 press]` | **ChatGPT ads pricing collapse** — $60 CPM (Feb, $200K min) → CPC at ~$25 effective CPM within ten weeks; penetration whiplashed 0.4% → 26.5% → 0.05% → ~50% US. Nobody knows how to price single-slot AI answers yet. |
| 8/10 | `[T3 press]` | **Bots overtake humans (Cloudflare)** — 57.4% of HTTP requests now bot-initiated; "bots don't click on ads" — impression-based value chains erode. |
| 7/10 | `[T3 press]` | **ChatGPT ads targeting velocity** — custom audience upload + AI-generated ad variations shipped within weeks of each other; OpenAI is rebuilding the Google/Meta advertiser toolkit at speed. |

### Below threshold (rejected, with scores)

- 6/10 — Meta Adaptive Ranking Model `[T2]`: serving-efficiency play; impressive infra, limited modeling applicability for our team size.
- 6/10 — Pinterest Beyond Two Towers `[T2]`, 5/10 — Pinterest GPU-serving two-tower `[T2]`: serving-stack re-architecture; revisit if we move lightweight ranking to GPU.
- 6/10 — Pinterest shopping conversion candidate generation `[T2]`: solid but incremental vs what the inventory already covers.
- 4/10 — AppLovin AI video creative `[T4, pending]`: paywalled teaser, creative-side, no modeling content.
- **3/10 — Netflix ads event processing pipeline `[T2, infra]`: well-written infra work, but infra-track items score ≤4 for a modeling team by calibration anchor.**
- 3/10 — ChatGPT ads timeline (contexthints) `[T4]`: redundant with the cloro.dev item above; timeline aggregation adds no new signal.

*8 of 15 items cleared the threshold this period — expected for a hand-curated seed crawl. On routine weekly crawls, expect most weeks to look like:*

> No items met the threshold this period. Silence below threshold is a feature, not a failure.
