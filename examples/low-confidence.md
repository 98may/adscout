# Example 2 — Confidence downgrade path

> Generated by running the SKILL.md pipeline on the real corpus, 2026-07-16.

**Question:** *"What modeling techniques does AppLovin Axon use?"*

---

**No first-party sources available.**

The corpus contains no tier 1–2 source from AppLovin (no papers, no official engineering blog — AppLovin does not publish one for Axon). Everything below is from tier 3–4 material and confidence is capped at **low**.

## What the corpus actually supports

The only AppLovin source in the corpus is:

- *AppLovin launches AI-enabled video creative generation* — Mobile Dev Memo (Eric Seufert), 2026-03-24, **tier 4 (analyst)**, `full_text: pending` (subscriber paywall; only the public teaser is stored).

From that teaser, the corpus supports exactly one claim: AppLovin has been operating an early-access program for **fully automated generation of interactive/video ads for eCommerce advertisers** and is expanding it. That is a *creative-generation* capability, not a disclosed Axon modeling technique.

## Answer

**The corpus cannot answer what modeling techniques Axon uses.** No source in the corpus describes Axon's architecture, features, training setup, or auction mechanics. Public claims elsewhere about Axon (e.g. "self-learning RL bidder") circulate in analyst commentary — **not from corpus** — and would inherit tier-4 status anyway.

```
confidence: low — no tier 1-2 sources exist in the corpus for this company;
            the single tier-4 source is paywalled (summary only) and does not
            discuss Axon internals.
```

**Suggested next step:** Ingest AppLovin's quarterly shareholder letters (tier 3) via `python crawl.py --url <letter-url> --tier 3 --lang en --track modeling` — they are the closest thing to first-party technical disclosure AppLovin produces.
